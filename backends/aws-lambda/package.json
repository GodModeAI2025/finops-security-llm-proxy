{
  "name": "llm-proxy-lambda",
  "version": "1.0.0",
  "description": "LLM API Proxy — AWS Lambda + DynamoDB + Secrets Manager",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "npx cdk deploy",
    "synth": "npx cdk synth"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "@aws-sdk/client-secrets-manager": "^3.500.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "aws-cdk-lib": "^2.130.0",
    "constructs": "^10.3.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
