{
  "name": "llm-proxy-cloudflare",
  "version": "1.0.0",
  "description": "LLM API Proxy — Cloudflare Workers + KV + Durable Objects",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.60.0"
  }
}
